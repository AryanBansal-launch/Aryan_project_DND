# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Instructions for Google OAuth Setup:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select an existing one
# 3. Enable the Google+ API
# 4. Go to "Credentials" and create OAuth 2.0 Client IDs
# 5. Set authorized redirect URIs to:
#    - http://localhost:3000/api/auth/callback/google (for development)
#    - https://yourdomain.com/api/auth/callback/google (for production)
# 6. Copy the Client ID and Client Secret to the variables above

# Database Configuration (NeonDB PostgreSQL)
# Get this from your Neon Dashboard: https://neon.tech/
DATABASE_URL=postgresql://[user]:[password]@[host]/[database]?sslmode=require

# Instructions for NeonDB Setup:
# 1. Sign up at https://neon.tech/
# 2. Create a new project
# 3. Copy the connection string from your project dashboard
# 4. Paste it as the DATABASE_URL above
# 5. Run the database initialization script:
#    psql $DATABASE_URL -f scripts/init-db.sql

# Algolia Search Configuration
# Get these from Algolia Dashboard: https://dashboard.algolia.com/
NEXT_PUBLIC_ALGOLIA_APP_ID=your-algolia-app-id
NEXT_PUBLIC_ALGOLIA_SEARCH_KEY=your-search-only-api-key
NEXT_PUBLIC_ALGOLIA_INDEX_NAME=job

# Instructions for Algolia Setup:
# 1. Sign up at https://www.algolia.com/
# 2. Create a new Application
# 3. Go to Settings → API Keys
# 4. Copy Application ID and Search-Only API Key
# 5. The index name should match what you configured in Contentstack Automate

# Admin Panel Protection (Edge Function)
# These credentials protect the /admin route with Basic HTTP Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password

# Instructions for Admin Protection:
# 1. Set a strong username and password above
# 2. When accessing /admin, you'll see a browser login prompt
# 3. Enter the credentials to access the admin panel
# 4. This runs as an edge function on Contentstack Launch

# Contentstack Personalize Configuration
# Get this from Contentstack Personalize dashboard
NEXT_PUBLIC_CONTENTSTACK_PERSONALIZE_PROJECT_UID=your-personalize-project-uid

# Instructions for Personalize Setup:
# 1. Go to Contentstack → Personalize
# 2. Create or select a Project
# 3. Copy the Project UID from the project settings
# 4. This enables behavior-based content personalization

# Contentstack Webhook Configuration (New Job Notifications)
# Secret to verify webhook requests from Contentstack
CONTENTSTACK_WEBHOOK_SECRET=your-webhook-secret-here

# Contentstack Automate webhook URL for sending new job emails
# Create an Automate workflow with Email connector and get its webhook trigger URL
CONTENTSTACK_NEW_JOB_EMAIL_WEBHOOK=https://automations-api.contentstack.com/your-workflow-url

# Instructions for New Job Email Notifications:
# 1. Go to Contentstack → Settings → Webhooks
# 2. Create a new webhook:
#    - Name: "New Job Notification"  
#    - URL: https://your-domain.com/api/webhooks/new-job
#    - Events: Select "Entry" → "Publish" for "Job" content type
#    - Headers: Add "x-webhook-secret" with your secret value
# 3. Create a Contentstack Automate workflow:
#    - Trigger: Webhook
#    - Action: Send Email (configure your email template)
#    - Copy the webhook trigger URL to CONTENTSTACK_NEW_JOB_EMAIL_WEBHOOK
